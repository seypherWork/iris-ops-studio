services:
  iris:
    build:
      context: .
    hostname: iris-ops-studio
    environment:
      ISC_DATA_DIRECTORY: /durable/iris
    ports:
      - "127.0.0.1:1972:1972"
      - "127.0.0.1:52773:52773"
    volumes:
      - iris-data:/durable

volumes:
  iris-data:
