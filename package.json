{
  "name": "iris-ops-studio",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "A safety-first operations console for InterSystems IRIS SysAdmin APIs",
  "scripts": {
    "start": "node mock/server.mjs",
    "test": "node --test",
    "check": "node --check web/assets/api.js && node --check web/assets/explorer.js && node --check web/assets/operations.js && node --check web/assets/app.js && node --test"
  },
  "engines": {
    "node": ">=22"
  }
}
